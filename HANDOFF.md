# Handoff — superseded

**See [`AGENTS.md`](./AGENTS.md).**

This file was written during an earlier session and is now out of date in ways that
would actively mislead:

- It describes the case-study carousel and detail pages as unshipped work in progress.
  Both are live on shubhamdesign.com.
- It lists the theme extraction and `embla-carousel` as "check whether it's actually
  wired in". They are, and Embla replaced the hand-rolled carousel entirely.
- It points at `public/trail-images/`. The cursor-trail images live in
  `src/trail-images/`.

Its environment and infra notes (Figma Desktop Bridge, Vercel Deployment Protection,
GoDaddy domain forwarding) were still accurate and have been carried into `AGENTS.md`.
